<template name="loadmore">
    <i-load-more wx:if="{{status== 0}}" tip="上拉加载更多" loading="{{ false }}" />
    <i-load-more wx:elif="{{status== 1}}" tip="正在加载中" loading="{{ true }}" />
    <i-load-more wx:elif="{{status== 2}}" tip="真棒，已经到底儿啦~~" loading="{{ false }}" />
</template>