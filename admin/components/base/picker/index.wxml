<!-- mask -->
<cm-mask id="sdai-mask" catchtap='hide'></cm-mask>
<!-- 下拉层 -->
<view catchtouchmove='nullFun' hidden="{{hidden}}" class="actionsheet {{ out ? 'out' : ''}} {{ enter ? 'enter' : '' }}">
  <slot></slot>
</view>

